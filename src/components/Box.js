import * as React from "react";
import { StyleSheet, View } from "react-native"

const Box = () => {
  return (
    <></>
  )
}

const styles = StyleSheet.create({
  box: {
    backgroundColor: 'red',
    height: 50,
    width: '100%',
    flex: 1
  },
  container: {
    marginTop: 50,
  },
  bigBlue: {
    color: 'blue',
    fontWeight: 'bold',
    fontSize: 30,
  },
  red: {
    color: 'red',
  },
});

export default Box